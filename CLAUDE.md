# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

[Add a brief description of what this project does and its main purpose]

## Development Commands

### Setup
```bash
# Initial setup commands
[e.g., npm install, pip install -r requirements.txt, etc.]
```

### Build
```bash
# Build commands
[e.g., npm run build, make build, etc.]
```

### Testing
```bash
# Run all tests
[e.g., npm test, pytest, etc.]

# Run a single test file
[e.g., npm test -- path/to/test, pytest path/to/test.py, etc.]

# Run tests in watch mode
[e.g., npm test -- --watch, etc.]
```

### Linting
```bash
# Lint code
[e.g., npm run lint, eslint ., etc.]

# Auto-fix linting issues
[e.g., npm run lint:fix, eslint . --fix, etc.]
```

### Running the Application
```bash
# Development mode
[e.g., npm run dev, python main.py, etc.]

# Production mode
[e.g., npm start, etc.]
```

## Architecture

### Project Structure
[Describe the high-level organization of the codebase]

### Key Components
[Describe the main architectural components and how they interact]

### Data Flow
[Explain how data moves through the system]

### Important Patterns
[Document any critical patterns, conventions, or architectural decisions that aren't obvious from reading individual files]

## Configuration

[Document any important configuration files, environment variables, or setup requirements]

## Dependencies

[Note any critical dependencies or external services the project relies on]
